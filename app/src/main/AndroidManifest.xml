<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.creater.shopping">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".activity.DashBoardAdmin"
            android:label="Admin DashBoard"
            ></activity>
        <activity android:name=".activity.savedList" />
        <activity android:name=".activity.CreateList" />
        <activity
            android:name=".activity.Dashboard"
            android:label="DashBoard" />
        <activity android:name=".activity.final_list" />
        <activity android:name=".activity.shopping_fire_data_set" />
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
    </application>

</manifest>